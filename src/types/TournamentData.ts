interface TournamentData {
  start: string; // Date-time in string format
  game: string;
  buyin: string;
  name: string;
  id: string;
  status: string;
  enrolled: string;
}

export default TournamentData;
